# Implementation Plans

`docs/PLAN.md` is the canonical execution plan for this project.

This directory is the governance framework's numbered-plan surface for
tracking individual implementation work items once they begin. It does not
replace or compete with `docs/PLAN.md`.

## Active Plans

| — | No active plans yet | — | — | — |

## Creating a New Plan

1. Copy `TEMPLATE.md` to `NN_name.md`
2. Fill in gap, steps, required tests
3. Add to this index
4. Commit with `[Plan #N]` prefix

## Trivial Changes

Not everything needs a plan. Use `[Trivial]` for:
- Less than 20 lines changed
- No changes to `src/` (production code)
- No new files created
