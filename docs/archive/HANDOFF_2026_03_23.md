# Session Handoff — 2026-03-23

## What Was Done (27 commits)

### Strategic Review & Thesis Validation
- Fixed circular thesis validation: cross-family models (Gemini, GPT-5-nano, DeepSeek) + distinct reasoning frames (verification_first, structured_decomposition, step_back_abstraction)
- ADR-0005: cross-family models required
- Controlled comparison vs single-shot synthesis: pipeline wins 3/4 with GPT-5-nano judge
- Phase-boundary integration tests: 30 tests, all passing

### Critical Fixes
- Dispute severity calibration (ADR-0005): schema Field(description) guidance + model bump to gemini-2.5-flash. Fixed: 0 decision-critical disputes → 3-5 per run
- Hallucinated evidence ID stripping at extraction time (code-level, not just prompt)
- Hallucinated claim ID stripping in synthesis (code-level)
- Trace success flag ordering bug (set before write_outputs)
- Silent fallback in ingest.py → explicit ValidationError catch

### SOTA Comparison
- GPT-Researcher: pipeline wins 24 vs 15 (fair comparison, no provenance bias)
- Perplexity sonar-pro: pipeline wins 24 vs 15
- Perplexity sonar-deep-research: **pipeline wins 23 vs 22** (after analytical mode)
- Competitive analysis documented in `docs/COMPETITIVE_ANALYSIS.md`
- Perplexity's synthesis prompt analyzed (leaked prompt)

### Phase A: Question Decomposition (v2 roadmap)
- Implemented: `QuestionDecomposition`, `SubQuestion` schemas (SQ- IDs)
- `decompose.py`: LLM decomposes question into typed sub-questions
- Sub-questions drive per-sub-question search queries
- Analysts receive sub-questions as structured context
- Long report receives optimization axes as organizing framework
- ADR-0006: design decisions (model, config key, no backward compat, ID format)
- Config: `models.decomposition: "gemini/gemini-2.5-flash"`

### Analytical Synthesis Mode
- Config: `synthesis_mode: "analytical" | "grounded"`
- Analytical: synthesize inferences beyond sources, marked with `[analytical inference]`
- Grounded: only render claims from ledger (original behavior)
- Provenance always in trace/ledger regardless of mode — report is a rendering
- This was the change that flipped the Perplexity comparison from loss to win

### Infrastructure
- v0.1.0 tagged and pushed to GitHub (BrianMills2718/grounded-research, private)
- open_web_retrieval pushed to GitHub (was untracked)
- Parallel page fetching (asyncio.gather) in collect.py
- Config: num_queries 15, max_sources 50
- SOTA comparison scripts: run_gpt_researcher.py, run_perplexity.py, run_storm.py
- Fair comparison script (compare_fair.py): 5 dimensions, no provenance bias
- Feature status mapped: 22/34 scorecard features done, 7 partial, 6 not started

### Ecosystem Audit
- Honest audit: no real data has ever flowed between projects
- onto-canon CLAUDE.md corrected: test count, Q-code coverage, capability classifications
- project-meta ECOSYSTEM_STATUS.md: integration reality table added
- onto-canon test suite fixed: NLTK installed, 541 tests passing
- onto-canon integration plan written (Plan #2)

## Current State

- **Pipeline beats Perplexity deep research** (23 vs 22, analytical mode)
- **Cost: $0.06/run** (from observability DB, not estimates)
- **30 tests passing**, clean tree, everything pushed
- **v2 roadmap** in `docs/ROADMAP_V2.md` with 5 phases (A done, B-E planned)

## What To Do Next

### Immediate (same session)
1. **Run pipeline on other test questions** (PFAS, fasting, microplastics) with decomposition + analytical mode to verify improvements generalize
2. **Update COMPETITIVE_ANALYSIS.md** with v10 analytical mode results

### Short-term (next sessions)
3. **Phase B: Source quality scoring** — LLM-based, per Brian's critique. Per-sub-question evidence sufficiency checking.
4. **Phase C: Model fallback chains** — reliability improvement, independent of other phases
5. **Investigate why Perplexity still wins on completeness (5 vs 4)** — may need more evidence items per sub-question or better macro-economic prompting

### Long-term
6. **Phases D-E** from ROADMAP_V2.md (user steering, quality polish)
7. **Source reputation DB** (deferred in PROJECTS_DEFERRED/)
8. **onto-canon integration** when ready (Plan #2 written)

## Key Files Changed

```
src/grounded_research/
  models.py          — QuestionDecomposition, SubQuestion added
  decompose.py       — NEW: question decomposition
  collect.py         — parallel fetch, sub-question-driven queries
  analysts.py        — decomposition context passed to analysts
  export.py          — synthesis_mode, decomposition context
  canonicalize.py    — hallucinated evidence ID stripping, severity Field guidance
  ingest.py          — ValidationError catch
  verify.py          — (pre-existing Phase 4 changes committed)

config/config.yaml   — decomposition model, synthesis_mode, 50 sources
prompts/
  decompose.yaml     — NEW: decomposition prompt
  analyst.yaml       — sub-questions in user message
  long_report.yaml   — analytical mode, broader implications, optimization axes
  synthesis.yaml     — evidence context, quality rules
  dispute_classify.yaml — severity guidance

engine.py            — decomposition step, --decomposition flag, success flag fix
scripts/
  compare_fair.py    — NEW: fair comparison without provenance bias
  run_gpt_researcher.py — NEW: SOTA comparison
  run_perplexity.py  — NEW: SOTA comparison
  run_storm.py       — NEW: SOTA comparison (failed, their bug)
  single_shot_baseline.py — baseline comparison
  compare_outputs.py — updated: --judge, --output flags

docs/
  ROADMAP_V2.md      — NEW: 5-phase v2 plan
  FEATURE_STATUS.md  — NEW: 52 scorecard features mapped
  COMPETITIVE_ANALYSIS.md — NEW: vs GPT-Researcher, Perplexity, STORM
  adr/0005-*         — cross-family models
  adr/0006-*         — decomposition design decisions
  PLAN.md            — updated throughout
  notebooks/02_*     — Phase A planning notebook

tests/
  test_phase_boundaries.py — NEW: 28 phase-boundary tests
```
