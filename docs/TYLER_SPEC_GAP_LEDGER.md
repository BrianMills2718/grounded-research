# Tyler Spec Gap Ledger

This file is the canonical evidence ledger for code-vs-spec review against
Tyler's V1 packet.

Do not record prose-only findings here. Every row must include:

- `spec_id`
- `tyler_source`
- `tyler_requirement`
- `local_surface`
- `evidence`
- `classification`
- `severity`
- `owner`
- `next_action`

## Status

**State:** Partially populated

The population protocol is defined in:

- `docs/plans/tyler_spec_gap_audit_wave1.md`

## Ledger Columns

| spec_id | tyler_source | tyler_requirement | local_surface | evidence | classification | severity | owner | next_action |
|---|---|---|---|---|---|---|---|---|

## Rows

| spec_id | tyler_source | tyler_requirement | local_surface | evidence | classification | severity | owner | next_action |
|---|---|---|---|---|---|---|---|---|
| S1-VALIDATION-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 1 note; `4. V1_PROMPTS (1).md` notes that Stage 1v was removed | V1 has no decomposition validation stage; self-check prompt text and terminal review are the mitigation | `src/grounded_research/decompose.py` `decompose_question_tyler_v1()`; `engine.py` `run_pipeline_from_question()` | Fixed on 2026-04-08 by deleting the live validation/retry path, removing `validate_decomposition.yaml`, and cutting question-entry execution over to `decompose_question_tyler_v1()` only. Verified with `tests/test_tyler_v1_stage1_runtime.py` and `tests/test_engine_fixture_loading.py`. | local_divergence | high | grounded-research | verified_fixed |
| S2-QUERY-MODEL-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 Step 1; `4. V1_PROMPTS.md` Prompt Inventory + `## Stage 2 — Query Variant Generation (Orchestrator Template)` | Stage 2 query variant generation is orchestrator-expanded string templates, not a model call | `src/grounded_research/collect.py` `generate_search_queries_tyler_v1()`; `prompts/tyler_v1_query_diversification.yaml`; `config/config.yaml` `models.query_diversification` | Exhaustive audit on 2026-04-12 reopened this row. Tyler's Build Plan says `Generate 3-5 query variants per sub-question ... — string templates, not a model call`, and the Prompt packet repeats `Not a model prompt. String templates expanded by the orchestrator.` The live runtime still renders `prompts/tyler_v1_query_diversification.yaml` and calls `acall_llm_structured(...)` inside `generate_search_queries_tyler_v1()`, with a dedicated `models.query_diversification` config entry. Earlier status/ledger closure here was wrong. | local_divergence | critical | grounded-research | reopen_remediation |
| S2-ROUTING-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 2 Step 4 routing table | Route Tavily or Exa based on query type; only high-priority sub-questions use both in parallel | `src/grounded_research/collect.py` `_search_one()`; `src/grounded_research/tools/web_search.py` | Fixed on 2026-04-08 by routing typed Stage 2 query plans by provider and query role instead of sending every query to both providers. High-priority sub-questions keep the Exa semantic variant; Tavily keyword variants now stay on Tavily. Verified with `tests/test_collect.py::test_collect_evidence_routes_tyler_query_plans_by_provider_role`. | local_divergence | high | grounded-research | verified_fixed |
| S2-QUALITY-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 2 Steps 7-9 | Stage 2 quality scoring should compute final `quality_score` from authority lookup + freshness decay + staleness modifiers, not just a coarse tier mapping | `src/grounded_research/source_quality.py`; `src/grounded_research/collect.py` `build_tyler_evidence_package()` | Fixed on 2026-04-08 by adding a deterministic Stage 2 quality pipeline that computes `quality_score` from authority lookup, optional freshness blending, authority floor, and staleness penalties, then propagates that final score into Tyler Stage 2 sources. Verified with `tests/test_source_quality.py::test_quality_score_applies_freshness_and_staleness_modifiers` and `tests/test_tyler_v1_stage2_runtime.py::test_build_tyler_evidence_package_uses_tyler_findings`. | local_divergence | high | grounded-research | verified_fixed |
| S3-FRAME-MODEL-001 | `4. V1_PROMPTS.md` `## Reasoning Frame Assignments` + Stage 3 frame sections | Stage 3 prompt packet assigns Gemini to `structured_decomposition` and Claude Opus to `verification_first` | `config/config.yaml` `analyst_models` + `analyst_frames`; `src/grounded_research/analysts.py` `run_analysts_tyler_v1()` | The original local divergence was that default config paired Analyst B with Claude and Analyst C with Gemini. Fixed on 2026-04-08 by restoring the shipped default order in `config/config.yaml`; the runtime still zips models and frames positionally, but the positions now match the prompt packet's requested mapping. Verified with `tests/test_tyler_v1_stage3_runtime.py::test_tyler_stage3_primary_config_matches_recovery_contract`. Packet-level disagreement between the Build Plan and Prompt packet is tracked separately in `AMB-S3-FRAME-001`. | local_divergence | critical | grounded-research | verified_fixed |
| AMB-S3-FRAME-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 3 model list; `4. V1_PROMPTS.md` `## Reasoning Frame Assignments` discrepancy note | Tyler's packet is internally inconsistent about whether Claude Opus or Gemini owns `structured_decomposition` vs `verification_first` in Stage 3 | `config/config.yaml`; `prompts/tyler_v1_analyst.yaml`; `docs/TYLER_EXECUTION_STATUS.md` | Build Plan Stage 3 lists Claude Opus 4.6 as `critical frame (structured decomposition)` and Gemini 3.1 Pro as `verification frame (verification-first)`. The Prompt packet assigns Gemini to `structured_decomposition` and Claude to `verification_first`, and includes an explicit discrepancy note saying the Build Plan appears to have swapped them without stated reasoning. The live runtime currently follows the Prompt packet assignment. | tyler_ambiguity | medium | docs-only | document_local_interpretation |
| S3-MODEL-VERSION-001 | `3. V1_SCHEMAS (1).md` schema header model list; `1. V1_Build_Plan_Step_By_Step (1).md` Stage 1 and 3 model assignments | Tyler names Gemini 3.1 Pro for decomposition and the Stage 3 structured-decomposition analyst | `config/config.yaml` `models.decomposition`, `models.evidence_extraction`, `models.source_scoring`, `analyst_models[1]`; `llm_client/data/default_model_registry.json`; `output/tyler_exact_model_version_switch_wave1_palantir/llm_observability.db` | Fixed on 2026-04-09 in two slices. Shared follow-through landed first when `llm_client` PR #28 merged and added `openrouter/google/gemini-3.1-pro-preview` to the packaged registry with `5/5` Tyler-like native-schema successes. Then `grounded-research` switched the Tyler-named Gemini roles in `config/config.yaml` and completed the raw-question validation run `output/tyler_exact_model_version_switch_wave1_palantir`. The run-local observability DB proves `question_decomposition_tyler_v1` used `openrouter/google/gemini-3.1-pro-preview` once, `finding_extraction_tyler_v1` used it `52` times, and Analyst B `analyst_reasoning_tyler_v1` used it once; `query_diversification_tyler_v1` intentionally remained on `openrouter/google/gemini-2.5-pro` per the explicit plan. Final artifacts (`report.md`, `summary.md`, `trace.json`, `handoff.json`) were written successfully. | shared_infra_blocked | medium | llm_client + grounded-research | verified_fixed |
| STATUS-FRONTIER-RUNTIME-001 | Tyler Stage 3-6 model requirements plus local status rule that frontier parity cannot be claimed without a literal live run | The production frontier config must be proven in a real literal fixture-backed run; closure requires the run to succeed without hidden provider/runtime fallback carrying a stage | `config/config.yaml`; `engine.py`; `output/tyler_frontier_runtime_validation_wave1/*`; `output/tyler_frontier_runtime_validation_wave2_repeat/*`; `output/tyler_frontier_runtime_validation_wave2_palantir/*` | Three literal production-config fixture runs are now recorded. Wave 1 (`output/tyler_frontier_runtime_validation_wave1`) reached final synthesis and used intended primary models for Analyst A (`openrouter/openai/gpt-5.4`), Analyst B (`openrouter/google/gemini-2.5-pro`), Analyst C (`openrouter/anthropic/claude-opus-4.6`), Stage 4 claim extraction (`openrouter/openai/gpt-5.4`), Stage 5 arbitration (`openrouter/anthropic/claude-opus-4.6`), and Stage 6 synthesis (`openrouter/openai/gpt-5.4`), but Gamma / Claude Opus failed with `Successful Tyler Stage 3 claim C-16 must cite at least one source reference.` Wave 2 repeated the same PFAS fixture and then ran a second Palantir fixture; both additional runs passed cleanly with the same primary models and no pipeline warnings. Frontier Reliability Wave 3 then compared the raw observability records and showed the key distinction: the failed Wave 1 Gamma call had **no transport error, no schema error, and no runtime fallback**, but its raw successful response included one claim (`C-16`) with empty `source_references`, which the local citation floor rejected. The passing repeat used the same stack and produced a cited `C-16`. Tyler Frontier Model Policy Wave 1 then set the decision rule: keep the Tyler-intended primary stack unless the same model-role pair fails the same quality floor in `2/3` identical reruns, the same failure mode appears on `2` distinct fixtures, or a shared runtime defect is proven. This row is now treated as a documented operational watch item rather than an active implementation blocker. Evidence: all three run directories' `trace.json`, `summary.md`, and run-local `llm_observability.db` files. | shared_infra_blocked | medium | shared model availability + config policy | operational_watch_reopen_on_threshold |
| S5-QUERY-ROLES-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 5 Steps 1-2; `4. V1_PROMPTS (1).md` Stage 5 neutral verification query template | Stage 5 must generate: neutral question, support query for the weaker position, authoritative-source query, and dated query only when recency-sensitive | `src/grounded_research/verify.py` `_build_tyler_verification_queries()` | Fixed on 2026-04-08. A later direct code review found the live path still used the old limitations/refutation builder, so the row was reopened and corrected in Phase 6 on 2026-04-08 by introducing typed `Stage5QueryPlan` objects and restoring Tyler's neutral, weaker-position, authoritative, and optional dated roles in the live runtime. Verified with `tests/test_verify.py::test_build_tyler_verification_queries_matches_literal_query_roles` and `tests/test_verify.py::test_build_tyler_verification_queries_adds_dated_search_only_when_time_sensitive`. | local_divergence | high | grounded-research | verified_fixed |
| S5-SEARCH-PARAMS-001 | `4. V1_PROMPTS (1).md` Stage 5 search parameters note; `1. V1_Build_Plan_Step_By_Step (1).md` Stage 5 Step 2 | Stage 5 should execute Tavily `search_depth=\"advanced\"` with `chunks_per_source=3`, and use `include_domains` when an authoritative domain is known | `src/grounded_research/verify.py` `_collect_fresh_evidence_for_dispute()` + `_build_tyler_verification_queries()`; `src/grounded_research/tools/web_search.py`; `open_web_retrieval` `SearchQuery` | Originally recorded as shared-infra-blocked. After `open_web_retrieval` Plan #15 landed depth/detail/domain controls, the real remaining gap was local consumption. Fixed on 2026-04-08 by routing Stage 5 through typed `Stage5QueryPlan` objects that pass `provider_override=\"tavily\"`, `search_depth=\"advanced\"`, `result_detail=\"chunks\"`, `detail_budget=3`, and `domains_allow` for authoritative-domain queries. Verified with `tests/test_verify.py::test_collect_fresh_evidence_uses_verification_search_trace_metadata`. | local_divergence | high | grounded-research | verified_fixed |
| S5-ROUND-CAP-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 5 budget note | Stage 5 has a hard cap of max 2 rounds total | `src/grounded_research/config.py` depth profiles; `src/grounded_research/verify.py` `verify_disputes_tyler_v1()` | Tyler says `Max 3 queries per disputed claim. Max 2 rounds total.` The original divergence was that live verification read `arbitration_max_rounds` directly from depth config and the `thorough` profile allowed `3`. Fixed on 2026-04-08 by clamping live verification to `min(configured_rounds, 2)` and verifying it with `tests/test_verify.py::test_verify_disputes_tyler_v1_caps_rounds_at_two`. | local_divergence | medium | grounded-research | verified_fixed |
| S2-TAVILY-DEPTH-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 2 Step 4 routing table | Stage 2 default keyword queries should use Tavily `search_depth=\"basic\"`; only Stage 5 targeted verification should force `advanced` | `open_web_retrieval` Tavily adapter; `grounded_research.tools.web_search.search_web()` | Fixed after `open_web_retrieval` Plan #15 added typed `search_depth`, `result_detail`, `detail_budget`, and domain controls to `SearchQuery`. The shared Tavily adapter now honors `basic` vs `advanced`, and `grounded-research` Stage 2 query plans request `basic` for keyword/practitioner/contrarian queries while Stage 5 requests `advanced`. Verified by `open_web_retrieval/tests/test_adapters.py::test_summary_detail_without_explicit_depth_uses_basic`, `grounded_research/tests/test_web_search.py::test_search_web_forwards_shared_retrieval_controls`, and `grounded_research/tests/test_collect.py::test_collect_evidence_routes_tyler_query_plans_by_provider_role`. | shared_infra_blocked | high | open_web_retrieval | verified_fixed |
| S2-EXA-CONTROLS-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 2 Steps 2-4 | Stage 2 Exa searches should support semantic/deep routing, `systemPrompt` control, and academic category routing | `open_web_retrieval` `SearchQuery`; `open_web_retrieval` Exa adapter; `src/grounded_research/collect.py`; `src/grounded_research/tools/web_search.py` | Fixed in two steps on 2026-04-08. `open_web_retrieval` Plan #15 closed shared depth/detail/corpus controls, and Plan #16 added generic `retrieval_instruction` support mapped to Exa `systemPrompt`. `grounded-research` Phase 7 then passed that field through the live Stage 2 semantic query path using typed `Stage2QueryPlan` objects. Verified with `open_web_retrieval/tests/test_adapters.py::TestExaAdapter::test_search_honors_retrieval_instruction`, `grounded_research/tests/test_web_search.py::test_search_web_exa_forwards_retrieval_instruction`, `grounded_research/tests/test_collect.py::test_collect_evidence_routes_tyler_query_plans_by_provider_role`, and `grounded_research/tests/test_tyler_v1_stage2_runtime.py::test_generate_search_queries_tyler_v1_returns_routed_query_plans`. | shared_infra_blocked | medium | open_web_retrieval + grounded-research | verified_fixed |
| S6-MODEL-POLICY-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 6b model note | Stage 6b must use a synthesis model that did not dominate earlier stages; Tyler leaves the exact winner TBD between Claude Opus and GPT-5.4 | `config/config.yaml` `models.synthesis`; `src/grounded_research/export.py` `generate_tyler_synthesis_report()` | Tyler says Stage 6b is `TBD` but `must not be the model that dominated earlier stages`. The original divergence was that Stage 6 used `get_model(\"synthesis\")` directly with no dominance check. Fixed on 2026-04-08 by introducing an explicit non-dominant model selector that prefers a configured alternate and fails loud when only dominant synthesis options remain. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_uses_non_dominant_synthesis_model`. | local_divergence | medium | grounded-research | verified_fixed |
| S4-ORDER-RANDOMIZATION-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 4 primacy-bias mitigation | Stage 4 must randomize the presentation order of Analyst A/B/C before each claim-extraction call so no analyst is systematically advantaged by prompt order | `src/grounded_research/canonicalize.py` `canonicalize_tyler_v1()` | Tyler explicitly says `Randomize the presentation order of Analyst A/B/C before each Stage 4 call.` The original divergence was that live code forwarded `tyler_stage_3_results` to the prompt in fixed list order and reused that order on retry. Fixed on 2026-04-08 by introducing a pure Stage 4 shuffle helper and rebuilding prompt messages from a fresh randomized order for both the primary call and retry path. Verified with `tests/test_canonicalize.py::test_randomize_stage4_analysis_order_preserves_aliases_without_mutating_input`, `tests/test_canonicalize.py::test_canonicalize_tyler_v1_randomizes_stage4_prompt_input`, and `tests/test_canonicalize.py::test_canonicalize_tyler_v1_rerenders_retry_from_fresh_randomized_order`. | local_divergence | high | grounded-research | verified_fixed |
| S5-ORDER-RANDOMIZATION-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 4 primacy-bias mitigation note for Stage 5; `4. V1_PROMPTS (1).md` Stage 5 notes | Stage 5 arbitration should randomize analyst-position order per dispute before rendering the arbitration prompt | `src/grounded_research/verify.py` `arbitrate_dispute_tyler_v1()` | Tyler says `For Stage 5 arbitration calls: also randomize the order of analyst positions per dispute.` The original divergence was that live code forwarded `dispute.model_dump(mode=\"json\")` directly into the Stage 5 prompt with no shuffle. Fixed on 2026-04-08 by introducing a pure prompt-payload helper that randomizes `model_positions` before rendering, verified with `tests/test_verify.py::test_randomize_dispute_model_positions_preserves_content_and_changes_order` and `tests/test_verify.py::test_arbitrate_dispute_tyler_v1_passes_randomized_positions_to_prompt`. | local_divergence | high | grounded-research | verified_fixed |
| S6A-STEERING-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 6a | Stage 6a should run after Stage 5 (if Stage 5 ran), inspect the updated dispute queue, and ask the user about unresolved preference-weighted/spec-ambiguity/other disputes | `engine.py` `run_pipeline()`; `src/grounded_research/tyler_v1_adapters.py` Stage 4 routing normalization | Tyler says Stage 6a is `Before synthesis` and checks the `updated dispute queue (post-Stage 5 if it ran)`. The original divergence was that live pipeline ran steering before Stage 5 and filtered only `unresolved`, which skipped `deferred_to_user` disputes. Fixed on 2026-04-08 by moving Stage 6a after Stage 5, selecting against the latest queue, and including `deferred_to_user` Tyler-routed disputes. Verified with `tests/test_engine.py::test_select_stage6a_steering_disputes_uses_latest_tyler_statuses` and `tests/test_engine.py::test_run_pipeline_collects_guidance_from_stage5_deferred_queue`. | local_divergence | critical | grounded-research | verified_fixed |
| S6-EVIDENCE-CONTEXT-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 6b key evidence sources; `4. V1_PROMPTS (1).md` Stage 6b USER template | Stage 6 synthesis should receive the sources that contributed to dispute resolution, including Stage 5 targeted-verification sources | `src/grounded_research/export.py` `generate_tyler_synthesis_report()` | Tyler's Stage 6b prompt expects `KEY EVIDENCE SOURCES` to include sources that contributed to disputes and recommendation. The original divergence was that live code built `top_sources` only from `state.evidence_bundle.sources`, which excluded Stage 5 `additional_sources`. Fixed on 2026-04-08 by merging Stage 2 and Stage 5 source inventories into one Stage 6 top-source assembly helper. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_includes_stage5_additional_sources_in_top_sources`. | local_divergence | high | grounded-research | verified_fixed |
| S6-COMPACTION-001 | `1. V1_Build_Plan_Step_By_Step (1).md` Stage 6b Step 1; `4. V1_PROMPTS (1).md` Context Compaction section | Stage 6 should compact prior-stage artifacts when synthesis input exceeds ~80K characters, using Tyler's priority order and front/back anchoring | `engine.py` evidence compression call; `src/grounded_research/export.py` `generate_tyler_synthesis_report()` | Tyler specifies a pre-synthesis compaction step triggered by total input size and ordered by claim/dispute importance. The original divergence was that `generate_tyler_synthesis_report()` rendered all Stage 6 inputs directly with no char-budget compaction. Fixed on 2026-04-08 by adding a Stage 6 prompt-input compaction helper that triggers on a configurable char-count limit and compresses lower-priority context first. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_compacts_noncritical_claims_when_over_limit`. | local_divergence | medium | grounded-research | verified_fixed |
| EXT-SCHEMA-001 | Tyler schema packet plus Tyler constraints 4, 8, and 9 as documented in later local compliance notes | Additive lineage fields may remain if they do not conflict with Tyler and are documented as extensions rather than literal schema parity | `src/grounded_research/tyler_v1_models.py` `ClaimLedgerEntry`, `ClaimStatusUpdate` | Local schema adds `status_at_extraction`, `is_provisional`, and `basis_for_change` at [tyler_v1_models.py](/home/brian/projects/grounded-research/src/grounded_research/tyler_v1_models.py#L233) through [tyler_v1_models.py](/home/brian/projects/grounded-research/src/grounded_research/tyler_v1_models.py#L240) and [tyler_v1_models.py](/home/brian/projects/grounded-research/src/grounded_research/tyler_v1_models.py#L318) through [tyler_v1_models.py](/home/brian/projects/grounded-research/src/grounded_research/tyler_v1_models.py#L332). Tyler's schema markdown does not include them, but they do not conflict with the runtime contract and are already described in local compliance docs. | extension | low | docs-only | document_extension |
| DOC-S2-QUERY-001 | Tyler packet plus live repo prompt inventory | Status docs should identify the real active Stage 2 query surface | `docs/TYLER_LITERAL_PROMPT_FIDELITY_AUDIT.md`; `docs/TYLER_LITERAL_PARITY_AUDIT.md`; `docs/TYLER_EXECUTION_STATUS.md` | Fixed on 2026-04-08 by landing the missing `prompts/tyler_v1_query_diversification.yaml` file and reconciling the status docs so they describe the actual active Stage 2 query surface. | stale_doc | high | docs-only | verified_fixed |
| DOC-S2-QUERY-002 | Tyler Build Plan Stage 2 Step 1 and prompt package Stage 2 query diversification | Tyler does not specify string-template query generation as the canonical Stage 2 implementation | `docs/TYLER_EXECUTION_STATUS.md` | Fixed on 2026-04-08 by removing the stale string-template claim from the status surface and describing the live lightweight-model query-diversification path instead. | stale_doc | high | docs-only | verified_fixed |
| DOC-S3-MODEL-001 | Tyler Build Plan Stage 3 and prompt package Frame B/C assignments | If Stage 3 model assignments are not Tyler-literal, status docs must not claim they are | `docs/TYLER_EXECUTION_STATUS.md` | Fixed on 2026-04-08 by updating the default B/C frame-model mapping and reconciling the status surface so it distinguishes fixed local frame ordering from still-open exact frontier-model version parity. | stale_doc | medium | docs-only | verified_fixed |
| DOC-ROADMAP-001 | Tyler packet plus active audit/status surfaces | The roadmap current-state summary must not describe pre-audit architecture that the ledger later proved false | `docs/ROADMAP.md` | Fixed on 2026-04-09 by rewriting the roadmap current-state summary to describe the actual Tyler-native runtime, the satisfied three-case eval gate, the merged Gemini shared fix, and the still-open frontier model-version / model-policy rows instead of the earlier pre-audit architecture snapshot. | stale_doc | medium | docs-only | verified_fixed |
| DOC-README-001 | Tyler packet plus active audit/status surfaces | README must not describe Stage 3 model parity as a generic “closest available” note once the live family/role mapping has been corrected and the remaining gap is only exact Gemini version parity | `README.md` | Fixed on 2026-04-09 by updating README feature notes to distinguish the landed Stage 3 family/role mapping from the still-open exact Gemini 3.1 Pro substitution row. | stale_doc | medium | docs-only | verified_fixed |
| DOC-FEATURE-001 | Tyler packet plus active audit/status surfaces | Feature status notes must not preserve pre-audit runtime descriptions when the live Stage 2/3/6 behavior has changed materially | `docs/FEATURE_STATUS.md` | Fixed on 2026-04-09 by updating the assessment date and correcting the Stage 2 findings, Stage 3 analyst-model, and Stage 6 compaction notes to match the current Tyler-native runtime and open exact-model gap. | stale_doc | medium | docs-only | verified_fixed |
| DOC-STATUS-001 | Tyler packet plus active audit/status surfaces | Active status and ownership docs must name the exact remaining model-version parity row separately from the stochastic frontier reliability/policy row | `docs/TYLER_EXECUTION_STATUS.md`; `docs/TYLER_SHARED_INFRA_OWNERSHIP.md`; `docs/plans/tyler_faithful_execution_remainder.md` | Fixed on 2026-04-09 by splitting the exact Tyler Gemini model-version parity note from the separate frontier runtime/model-policy note and by pointing the exact-model row at a dedicated shared follow-up plan. | stale_doc | medium | docs-only | verified_fixed |
