# Tyler Spec Gap Ledger

> Provenance/status: Tyler review/provenance artifact. Preserve for audit.
> Some status claims may be superseded by the current machine-readable
> registry. For current status, cross-check `docs/MAINTAINER_START_HERE.md`,
> `docs/tyler_requirements.yaml`, and
> `docs/tyler_requirements_registry.json`.

This file is the canonical evidence ledger for code-vs-spec review against
Tyler's V1 packet.

Do not record prose-only findings here. Every row must include:

- `spec_id`
- `tyler_source`
- `tyler_requirement`
- `local_surface`
- `evidence`
- `classification`
- `severity`
- `owner`
- `next_action`

## Status

**State:** Partially populated

The population protocol is defined in:

- `docs/plans/archive/tyler_spec_gap_audit_wave1.md`

## Ledger Columns

| spec_id | tyler_source | tyler_requirement | local_surface | evidence | classification | severity | owner | next_action |
|---|---|---|---|---|---|---|---|---|

## Rows

| spec_id | tyler_source | tyler_requirement | local_surface | evidence | classification | severity | owner | next_action |
|---|---|---|---|---|---|---|---|---|
| S1-VALIDATION-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 1 note line 27; `2. V1_DESIGN.md` Stage 1 self-check lines 81 and 87; `4. V1_PROMPTS.md` V1 changes line 24 and Stage 1 self-check lines 143-148 | V1 has no decomposition validation stage; self-check prompt text and terminal review are the mitigation | `src/grounded_research/decompose.py` `decompose_question_tyler_v1()`; `engine.py` `run_pipeline_from_question()` | Fixed on 2026-04-08 by deleting the live validation/retry path, removing `validate_decomposition.yaml`, and cutting question-entry execution over to `decompose_question_tyler_v1()` only. Verified with `tests/test_tyler_v1_stage1_runtime.py` and `tests/test_engine_fixture_loading.py`. | local_divergence | high | grounded-research | verified_fixed |
| S2-QUERY-MODEL-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 Step 1 line 38; `2. V1_DESIGN.md` Stage 2 lines 93-95 and Source Strategy Tier 1 lines 269-273; `4. V1_PROMPTS.md` Prompt Inventory lines 102-107 and Stage 2 Query Variant Generation lines 263-266 | Stage 2 query-variant generation is an orchestrator/string-template step, not a model call | `src/grounded_research/collect.py` `generate_search_queries_tyler_v1()`; `config/config.yaml`; `tests/test_tyler_v1_stage2_runtime.py` | Reopened on 2026-06-25 by the coverage-quality anchor pass. The raw Tyler packet says Stage 2 Step 1 is `string templates, not a model call`, Prompt Inventory classifies query variant generation under `Orchestrator templates (string generation, no model call)`, and the prompt packet's Stage 2 query-variant section says `Not a model prompt. String templates expanded by the orchestrator.` Fixed on 2026-06-25 by replacing the live `acall_llm_structured(...)` query-diversification path with deterministic template expansion in `generate_search_queries_tyler_v1()`, removing the dormant `query_diversification` model/fallback config and prompt file, and adding `tests/test_tyler_v1_stage2_runtime.py::test_generate_search_queries_tyler_v1_returns_routed_query_plans`, which fails if Stage 2 query generation calls an LLM. | local_divergence | high | grounded-research | verified_fixed |
| S2-QUERY-VARIANTS-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 Step 1 lines 38-39; `2. V1_DESIGN.md` Stage 2 lines 93-95 and Source Strategy Tier 1 lines 269-273; `4. V1_PROMPTS.md` Stage 2 Query Variant Generation lines 263-285 | Stage 2 query variants should follow Tyler's string-template family for 3-5 variants with hard cap 4 queries per sub-question | `src/grounded_research/collect.py` `MAX_QUERIES_PER_SUB_QUESTION`; `src/grounded_research/collect.py` `generate_search_queries_tyler_v1()`; `src/grounded_research/models.py` `Stage2QueryPlan`; `tests/test_tyler_v1_stage2_runtime.py` | Reopened on 2026-06-25 by the coverage-quality anchor pass. Tyler's prompt packet enumerates formal/technical, practitioner-oriented, academic/research, colloquial/forum, and contrarian variants, with a hard cap of 4 queries per sub-question. Fixed on 2026-06-25 by emitting deterministic formal, practitioner, academic, and contrarian query plans under named constant `MAX_QUERIES_PER_SUB_QUESTION = 4`. High-priority sub-questions still exercise the audited Exa provider-control path for the academic/research query, while non-high-priority sub-questions stay Tavily-only. Verified by `tests/test_tyler_v1_stage2_runtime.py::test_generate_search_queries_tyler_v1_returns_routed_query_plans`. | local_divergence | high | grounded-research | verified_fixed |
| S2-ROUTING-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 tools line 33 and query execution lines 38-39; `2. V1_DESIGN.md` Stage 2 tools line 91 and Source Strategy lines 267-270 | Route Tavily or Exa based on query type; only high-priority sub-questions use both in parallel | `src/grounded_research/collect.py` `_search_one()`; `src/grounded_research/tools/web_search.py` | Fixed on 2026-04-08 by routing typed Stage 2 query plans by provider and query role instead of sending every query to both providers. High-priority sub-questions keep the Exa semantic variant; Tavily keyword variants now stay on Tavily. Verified with `tests/test_collect.py::test_collect_evidence_routes_tyler_query_plans_by_provider_role`. | local_divergence | high | grounded-research | verified_fixed |
| S2-QUALITY-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 quality scoring line 40; `2. V1_DESIGN.md` Stage 2 source-quality line 95 and Source Strategy line 273; `3. V1_SCHEMAS.md` Source.quality_score lines 160 and 182-187 | Stage 2 quality scoring should compute final `quality_score` from authority lookup + freshness decay + staleness modifiers, not just a coarse tier mapping | `src/grounded_research/source_quality.py`; `src/grounded_research/collect.py` `build_tyler_evidence_package()` | Fixed on 2026-04-08 by adding a deterministic Stage 2 quality pipeline that computes `quality_score` from authority lookup, optional freshness blending, authority floor, and staleness penalties, then propagates that final score into Tyler Stage 2 sources. Verified with `tests/test_source_quality.py::test_quality_score_applies_freshness_and_staleness_modifiers` and `tests/test_tyler_v1_stage2_runtime.py::test_build_tyler_evidence_package_uses_tyler_findings`. | local_divergence | high | grounded-research | verified_fixed |
| S3-FRAME-MODEL-001 | `4. V1_PROMPTS.md` Reasoning Frame Assignments lines 68-83 and Stage 3 frame sections lines 348, 376, and 400; `2. V1_DESIGN.md` Stage 3 frame rule lines 110-112 | Stage 3 prompt packet assigns Gemini to `structured_decomposition` and Claude Opus to `verification_first` | `config/config.yaml` `analyst_models` + `analyst_frames`; `src/grounded_research/analysts.py` `run_analysts_tyler_v1()` | The original local divergence was that default config paired Analyst B with Claude and Analyst C with Gemini. Fixed on 2026-04-08 by restoring the shipped default order in `config/config.yaml`; the runtime still zips models and frames positionally, but the positions now match the prompt packet's requested mapping. Verified with `tests/test_tyler_v1_stage3_runtime.py::test_tyler_stage3_primary_config_matches_recovery_contract`. Full packet audit on 2026-04-12 also confirmed the Build Plan's role labels are cosmetic: its parenthetical frame assignments match the Design and Prompt packets. | local_divergence | critical | grounded-research | verified_fixed |
| AMB-S3-FRAME-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 3 model list lines 57-64; `2. V1_DESIGN.md` Model Roles lines 198-214; `4. V1_PROMPTS.md` Reasoning Frame Assignments lines 68-83 | Stage 3 role labels should be interpreted by their parenthetical reasoning-frame names, not as contradictory model assignments | `docs/TYLER_EXECUTION_STATUS.md`; `docs/TYLER_FULL_SPEC_AUDIT_MATRIX.md`; `docs/TYLER_SYSTEMATIC_REVIEW_MATRIX.md` | Full packet audit on 2026-04-12 corrected an earlier false ambiguity. The Build Plan says Claude is the `critical frame (verification-first)` and Gemini is the `verification frame (structured decomposition)`. The Design packet says the assignments are defined in `PROMPTS.md`, and the Prompt packet explicitly states the role names are cosmetic labels while the parenthetical frame assignments are the authoritative mapping. Earlier docs treated this as a packet conflict; that interpretation was wrong. | stale_doc | medium | docs-only | verified_fixed |
| AMB-S2-REASONING-001 | `4. V1_PROMPTS.md` Global Prompt Conventions lines 39-64; `3. V1_SCHEMAS.md` Stage 2 Finding schema lines 144-153 | Tyler's shared prompt protocol implies a reasoning field everywhere, but Tyler's own Stage 2 `Finding` schema omits it | `prompts/tyler_v1_extract_findings.yaml`; `src/grounded_research/tyler_v1_models.py` `Finding`; `docs/TYLER_EXECUTION_STATUS.md` | The Prompt packet says `Append this block to every system message` and includes `REASONING REQUIREMENT: ... The reasoning field is not optional boilerplate`. But Tyler's Stage 2 `Finding` schema contains only `finding`, `evidence_label`, and optional `original_quote`. The local Stage 2 extraction prompt keeps the shared output protocol but omits the impossible reasoning-field line, which is a documented local interpretation of Tyler's internal contradiction rather than a silent divergence. | tyler_ambiguity | medium | docs-only | document_local_interpretation |
| AMB-UNIT-SCENARIO-001 | `2. V1_DESIGN.md` Unit of Analysis lines 285-291; `3. V1_SCHEMAS.md` Stage 6 SynthesisReport lines 470-493; `4. V1_PROMPTS.md` Stage 6 synthesis input/output lines 930-980 | Tyler's Design packet says the unit of analysis is claim-assumption-scenario tuples and that alternatives are bundles of claims, assumptions, and scenarios, but the later schema/prompt packet never materializes a first-class scenario object or field | `2026_0325_tyler_feedback/2. V1_DESIGN.md`; `src/grounded_research/tyler_v1_models.py` `SynthesisReport`, `PreservedAlternative`, `KeyAssumption`; `prompts/tyler_v1_synthesis.yaml` | The canonical Design packet says `Claim–assumption–scenario tuples, with claims as the operational core` and `Alternatives tracked as bundles of claims + assumptions + scenarios.` But the canonical Schema packet contains no `Scenario` model, no scenario field on `AnalysisObject`, `ClaimExtractionResult`, `VerificationResult`, or `SynthesisReport`, and the Prompt packet likewise contains no scenario surface. The live repo carries scenario-like information indirectly through `conditions_of_validity`, `decision_relevant_tradeoffs`, and `preserved_alternatives.conditions_for_preference`, which is a reasonable local interpretation of Tyler's later schema/prompt simplification, not proof that Tyler's earlier design sentence was fully materialized. | tyler_ambiguity | low | docs-only | document_local_interpretation |
| AMB-S6A-STATUS-001 | `3. V1_SCHEMAS.md` DisputeStatus enum lines 67-70 and Stage 6 interrupt trigger lines 569-580; `4. V1_PROMPTS.md` Stage 6b dispute status branches lines 1024-1037 | Tyler's Stage 6a packet is internally inconsistent on whether user-routed disputes remain `unresolved` or are marked `deferred_to_user` before synthesis | `engine.py` `_select_stage6a_steering_disputes()`; `src/grounded_research/tyler_v1_models.py` `DisputeStatus`; `docs/TYLER_EXECUTION_STATUS.md` | Tyler's schema enum says `DEFERRED_TO_USER = "deferred_to_user"  # Preference/ambiguity/other — surfaced to user via terminal interrupt`, and the Stage 6b prompt template branches on both `dispute.status == "deferred_to_user"` and `dispute.status == "unresolved"`. But the Build Plan, Design packet, and Schema pseudocode for the Stage 6a trigger all filter only `status == DisputeStatus.UNRESOLVED`. The live runtime keeps both statuses in `_select_stage6a_steering_disputes()` so routed disputes are not silently dropped. This is a documented local interpretation of Tyler's internal inconsistency rather than a silent divergence. | tyler_ambiguity | medium | docs-only | document_local_interpretation |
| SC-PIPELINESTATE-001 | `3. V1_SCHEMAS.md` Schema Categories lines 21-25 and PipelineState lines 515-550 | The trace file should serialize Tyler's `PipelineState` contract, including `stage_1_result` … `stage_6_result`, `stage_5_skipped`, `stage_6_user_input`, `errors`, and `total_cost_usd` | `engine.py` `run_pipeline()`; `src/grounded_research/models.py` `PipelineState`; `src/grounded_research/tyler_v1_models.py` `PipelineState`; `src/grounded_research/export.py` `write_outputs()`; `tests/test_export.py` | Fixed on 2026-04-13 in Phase 3 of the post-audit remediation wave. The live runtime now projects the repo-local execution state into Tyler's canonical `PipelineState` via `build_tyler_pipeline_state()` / `write_tyler_trace()`. `write_outputs()` no longer dumps the repo-local state object directly, and `engine.py` now uses the same Tyler trace helper for both successful runs and failure-path partial traces. Verified with `tests/test_export.py::test_write_outputs_writes_tyler_pipeline_state_trace`, `tests/test_export.py::test_write_tyler_trace_projects_partial_failure_state`, and focused `tests/test_export.py tests/test_engine.py`. | local_divergence | high | grounded-research | verified_fixed |
| S3-MODEL-VERSION-001 | `3. V1_SCHEMAS.md` schema category model list lines 14-18; `1. V1_Build_Plan_Step_By_Step.md` Stage 1 model lines 9-10 and Stage 3 model list lines 57-62; `4. V1_PROMPTS.md` Stage 3 frame assignment lines 72-74 and 95-97 | Tyler names Gemini 3.1 Pro for decomposition and the Stage 3 structured-decomposition analyst | `config/config.yaml` `models.decomposition`, `models.evidence_extraction`, `models.source_scoring`, `analyst_models[1]`; `llm_client/data/default_model_registry.json`; `output/tyler_exact_model_version_switch_wave1_palantir/llm_observability.db` | Fixed on 2026-04-09 in two slices. Shared follow-through landed first when `llm_client` PR #28 merged and added `openrouter/google/gemini-3.1-pro-preview` to the packaged registry with `5/5` Tyler-like native-schema successes. Then `grounded-research` switched the Tyler-named Gemini roles in `config/config.yaml` and completed the raw-question validation run `output/tyler_exact_model_version_switch_wave1_palantir`. The run-local observability DB proves `question_decomposition_tyler_v1` used `openrouter/google/gemini-3.1-pro-preview` once, `finding_extraction_tyler_v1` used it `52` times, and Analyst B `analyst_reasoning_tyler_v1` used it once. Final artifacts (`report.md`, `summary.md`, `trace.json`, `handoff.json`) were written successfully. | shared_infra_blocked | medium | llm_client + grounded-research | verified_fixed |
| STATUS-FRONTIER-RUNTIME-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 3 model list lines 57-62, Stage 6b model note line 167, and Stage 6b validation lines 184-186; local status rule that frontier parity cannot be claimed without a literal live run | The production frontier config must be proven in a real literal fixture-backed run; closure requires the run to succeed without hidden provider/runtime fallback carrying a stage | `config/config.yaml`; `engine.py`; `output/tyler_frontier_runtime_validation_wave1/*`; `output/tyler_frontier_runtime_validation_wave2_repeat/*`; `output/tyler_frontier_runtime_validation_wave2_palantir/*` | Three literal production-config fixture runs are now recorded. Wave 1 (`output/tyler_frontier_runtime_validation_wave1`) reached final synthesis and used intended primary models for Analyst A (`openrouter/openai/gpt-5.4`), Analyst B (`openrouter/google/gemini-2.5-pro`), Analyst C (`openrouter/anthropic/claude-opus-4.6`), Stage 4 claim extraction (`openrouter/openai/gpt-5.4`), Stage 5 arbitration (`openrouter/anthropic/claude-opus-4.6`), and Stage 6 synthesis (`openrouter/openai/gpt-5.4`), but Gamma / Claude Opus failed with `Successful Tyler Stage 3 claim C-16 must cite at least one source reference.` Wave 2 repeated the same PFAS fixture and then ran a second Palantir fixture; both additional runs passed cleanly with the same primary models and no pipeline warnings. Frontier Reliability Wave 3 then compared the raw observability records and showed the key distinction: the failed Wave 1 Gamma call had **no transport error, no schema error, and no runtime fallback**, but its raw successful response included one claim (`C-16`) with empty `source_references`, which the local citation floor rejected. The passing repeat used the same stack and produced a cited `C-16`. Tyler Frontier Model Policy Wave 1 then set the decision rule: keep the Tyler-intended primary stack unless the same model-role pair fails the same quality floor in `2/3` identical reruns, the same failure mode appears on `2` distinct fixtures, or a shared runtime defect is proven. This row is now treated as a documented operational watch item rather than an active implementation blocker. Evidence: all three run directories' `trace.json`, `summary.md`, and run-local `llm_observability.db` files. | shared_infra_blocked | medium | grounded-research config policy + llm_client model transport/observability | operational_watch_reopen_on_threshold |
| S5-QUERY-ROLES-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 5 search steps lines 132-143; `4. V1_PROMPTS.md` Counterfactual Query Generation lines 736-752 | Stage 5 must generate: neutral question, support query for the weaker position, authoritative-source query, and dated query only when recency-sensitive | `src/grounded_research/verify.py` `_build_tyler_verification_queries()` | Fixed on 2026-04-08. A later direct code review found the live path still used the old limitations/refutation builder, so the row was reopened and corrected in Phase 6 on 2026-04-08 by introducing typed `Stage5QueryPlan` objects and restoring Tyler's neutral, weaker-position, authoritative, and optional dated roles in the live runtime. Verified with `tests/test_verify.py::test_build_tyler_verification_queries_matches_literal_query_roles` and `tests/test_verify.py::test_build_tyler_verification_queries_adds_dated_search_only_when_time_sensitive`. | local_divergence | high | grounded-research | verified_fixed |
| S5-SEARCH-PARAMS-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 5 search and budget lines 132-143; `2. V1_DESIGN.md` Source Strategy Tier 2 lines 277-281; `4. V1_PROMPTS.md` Stage 5 query template lines 736-752 | Stage 5 should execute Tavily `search_depth=\"advanced\"` with `chunks_per_source=3`, and use `include_domains` when an authoritative domain is known | `src/grounded_research/verify.py` `_collect_fresh_evidence_for_dispute()` + `_build_tyler_verification_queries()`; `src/grounded_research/tools/web_search.py`; `open_web_retrieval` `SearchQuery` | Originally recorded as shared-infra-blocked. After `open_web_retrieval` Plan #15 landed depth/detail/domain controls, the real remaining gap was local consumption. Fixed on 2026-04-08 by routing Stage 5 through typed `Stage5QueryPlan` objects that pass `provider_override=\"tavily\"`, `search_depth=\"advanced\"`, `result_detail=\"chunks\"`, `detail_budget=3`, and `domains_allow` for authoritative-domain queries. Verified with `tests/test_verify.py::test_collect_fresh_evidence_uses_verification_search_trace_metadata`. | local_divergence | high | grounded-research | verified_fixed |
| S5-ROUND-CAP-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 5 budget line 139; `2. V1_DESIGN.md` Stage 5 budget line 167 and Source Strategy Tier 2 line 280; `3. V1_SCHEMAS.md` Stage 5 rounds lines 382 and 391 | Stage 5 has a hard cap of max 2 rounds total | `src/grounded_research/config.py` depth profiles; `src/grounded_research/verify.py` `verify_disputes_tyler_v1()` | Tyler says `Max 3 queries per disputed claim. Max 2 rounds total.` The original divergence was that live verification read `arbitration_max_rounds` directly from depth config and the `thorough` profile allowed `3`. Fixed on 2026-04-08 by clamping live verification to `min(configured_rounds, 2)` and verifying it with `tests/test_verify.py::test_verify_disputes_tyler_v1_caps_rounds_at_two`. | local_divergence | medium | grounded-research | verified_fixed |
| S2-TAVILY-DEPTH-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 tools and search execution lines 33 and 38-39; `2. V1_DESIGN.md` Source Strategy tools and Tier 1 lines 267-275 | Stage 2 default keyword queries should use Tavily `search_depth=\"basic\"`; only Stage 5 targeted verification should force `advanced` | `open_web_retrieval/src/open_web_retrieval/adapters/tavily.py`; `grounded_research.tools.web_search.search_web()` | Fixed after `open_web_retrieval` Plan #15 added typed `search_depth`, `result_detail`, and domain controls to `SearchQuery`. The shared Tavily adapter now honors `basic` vs `advanced`, and `grounded-research` Stage 2 query plans request `basic` for keyword/practitioner/contrarian queries while Stage 5 requests `advanced`. Verified by `open_web_retrieval/tests/test_adapters.py::test_summary_detail_without_explicit_depth_uses_basic`, `grounded_research/tests/test_web_search.py::test_search_web_forwards_shared_retrieval_controls`, and `grounded_research/tests/test_collect.py::test_collect_evidence_routes_tyler_query_plans_by_provider_role`. | shared_infra_blocked | high | open_web_retrieval | verified_fixed |
| S2-EXA-CONTROLS-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 2 tools and query/search steps lines 33 and 38-39; `2. V1_DESIGN.md` Source Strategy tools and Tier 1 lines 267-275; `4. V1_PROMPTS.md` Stage 2 academic query template lines 263-285 | Stage 2 Exa searches should support semantic/deep routing, `systemPrompt` control, and academic category routing | `open_web_retrieval` `SearchQuery`; `open_web_retrieval` Exa adapter; `src/grounded_research/collect.py`; `src/grounded_research/tools/web_search.py` | Fixed in two steps on 2026-04-08. `open_web_retrieval` Plan #15 closed shared depth/detail/corpus controls, and Plan #16 added generic `retrieval_instruction` support mapped to Exa `systemPrompt`. `grounded-research` Phase 7 then passed that field through the live Stage 2 semantic query path using typed `Stage2QueryPlan` objects. Verified with `open_web_retrieval/tests/test_adapters.py::TestExaAdapter::test_search_honors_retrieval_instruction`, `grounded_research/tests/test_web_search.py::test_search_web_exa_forwards_retrieval_instruction`, `grounded_research/tests/test_collect.py::test_collect_evidence_routes_tyler_query_plans_by_provider_role`, and `grounded_research/tests/test_tyler_v1_stage2_runtime.py::test_generate_search_queries_tyler_v1_returns_routed_query_plans`. | shared_infra_blocked | medium | open_web_retrieval + grounded-research | verified_fixed |
| S6-MODEL-POLICY-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 6b model note line 167; `2. V1_DESIGN.md` Model Roles line 210 | Stage 6b must use a synthesis model that did not dominate earlier stages; Tyler leaves the exact winner TBD between Claude Opus and GPT-5.4 | `config/config.yaml` `models.synthesis`; `src/grounded_research/export.py` `generate_tyler_synthesis_report()` | Tyler says Stage 6b is `TBD` but `must not be the model that dominated earlier stages`. The original divergence was that Stage 6 used `get_model(\"synthesis\")` directly with no dominance check. Fixed on 2026-04-08 by introducing an explicit non-dominant model selector that prefers a configured alternate and fails loud when only dominant synthesis options remain. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_uses_non_dominant_synthesis_model`. | local_divergence | medium | grounded-research | verified_fixed |
| S4-ORDER-RANDOMIZATION-001 | `4. V1_PROMPTS.md` Stage 4 user template randomization note lines 640-643 | Stage 4 must randomize the presentation order of Analyst A/B/C before each claim-extraction call so no analyst is systematically advantaged by prompt order | `src/grounded_research/canonicalize.py` `canonicalize_tyler_v1()` | Tyler explicitly says `Randomize the presentation order of Analyst A/B/C before each Stage 4 call.` The original divergence was that live code forwarded `tyler_stage_3_results` to the prompt in fixed list order and reused that order on retry. Fixed on 2026-04-08 by introducing a pure Stage 4 shuffle helper and rebuilding prompt messages from a fresh randomized order for both the primary call and retry path. Verified with `tests/test_canonicalize.py::test_randomize_stage4_analysis_order_preserves_aliases_without_mutating_input`, `tests/test_canonicalize.py::test_canonicalize_tyler_v1_randomizes_stage4_prompt_input`, and `tests/test_canonicalize.py::test_canonicalize_tyler_v1_rerenders_retry_from_fresh_randomized_order`. | local_divergence | high | grounded-research | verified_fixed |
| S5-ORDER-RANDOMIZATION-001 | `4. V1_PROMPTS.md` Stage 5 user template randomization note lines 826-829 and developer note line 1136 | Stage 5 arbitration should randomize analyst-position order per dispute before rendering the arbitration prompt | `src/grounded_research/verify.py` `arbitrate_dispute_tyler_v1()` | Tyler says `For Stage 5 arbitration calls: also randomize the order of analyst positions per dispute.` The original divergence was that live code forwarded `dispute.model_dump(mode=\"json\")` directly into the Stage 5 prompt with no shuffle. Fixed on 2026-04-08 by introducing a pure prompt-payload helper that randomizes `model_positions` before rendering, verified with `tests/test_verify.py::test_randomize_dispute_model_positions_preserves_content_and_changes_order` and `tests/test_verify.py::test_arbitrate_dispute_tyler_v1_passes_randomized_positions_to_prompt`. | local_divergence | high | grounded-research | verified_fixed |
| S6-PROMPT-VARS-001 | `4. V1_PROMPTS.md` Orchestrator-Constructed Variables lines 1124-1131 and Stage 6b user template lines 991-1047 | Stage 6 should preserve Tyler's orchestrator-constructed prompt variables, especially `decision_critical_claim_ids` and `user_response_for_dispute`, instead of replacing them with different local convenience inputs | `prompts/tyler_v1_synthesis.yaml`; `src/grounded_research/export.py` `generate_tyler_synthesis_report()`; `tests/test_prompt_templates.py::test_tyler_stage6_synthesis_prompt_renders_literal_contract`; `tests/test_export.py::test_generate_tyler_synthesis_report_passes_tyler_stage6_prompt_variables` | Fixed on 2026-04-13 in Phase 1 of the post-audit remediation wave. The Stage 6 prompt now renders from `claim_ledger` plus `decision_critical_claim_ids` and uses `user_response_for_dispute` for deferred-to-user disputes. `generate_tyler_synthesis_report()` now builds that Tyler-style variable surface directly instead of pre-splitting claims into local convenience subsets or passing `stage_6_user_input` through the prompt contract. Verified with prompt-render and export-path tests. | local_divergence | medium | grounded-research | verified_fixed |
| S5-S6-DATASTRUCT-001 | `4. V1_PROMPTS.md` Data Structure Conventions lines 1133-1136, Stage 5 claim-ledger access lines 817-824, and Stage 6 claim-ledger filtering lines 998-1009 | Stage 5 should render disputed claims from `claim_ledger[claim_id]` after the orchestrator converts the claim ledger to a dict keyed by claim ID, and Stage 6b should keep the claim ledger list plus `decision_critical_claim_ids` set-membership split rather than pre-materialized subsets | `prompts/tyler_v1_arbitration.yaml`; `prompts/tyler_v1_synthesis.yaml`; `src/grounded_research/verify.py` `arbitrate_dispute_tyler_v1()`; `src/grounded_research/export.py` `generate_tyler_synthesis_report()`; `tests/test_prompt_templates.py`; `tests/test_verify.py::test_arbitrate_dispute_tyler_v1_passes_claim_ledger_as_dict`; `tests/test_export.py::test_generate_tyler_synthesis_report_compacts_noncritical_claims_when_over_limit` | Fixed on 2026-04-13 in Phase 1 of the post-audit remediation wave. Stage 5 now passes a dict-keyed claim ledger to the arbitration prompt and the prompt uses Tyler's `claim_ledger[claim_id]` access pattern. Stage 6 compaction and rendering now preserve one `claim_ledger` list and `decision_critical_claim_ids` membership checks rather than pre-materialized decision-critical/non-critical subsets. Verified with prompt-render, arbitration, and export compaction tests. | local_divergence | medium | grounded-research | verified_fixed |
| S6A-STEERING-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 6a lines 151-161; `3. V1_SCHEMAS.md` Stage 6 interrupt trigger lines 569-580 | Stage 6a should run after Stage 5 (if Stage 5 ran), inspect the updated dispute queue, and ask the user about unresolved preference-weighted/spec-ambiguity/other disputes | `engine.py` `run_pipeline()`; `src/grounded_research/tyler_v1_adapters.py` Stage 4 routing normalization | Tyler says Stage 6a is `Before synthesis` and checks the `updated dispute queue (post-Stage 5 if it ran)`. The original divergence was that live pipeline ran steering before Stage 5 and filtered only `unresolved`, which skipped `deferred_to_user` disputes. Fixed on 2026-04-08 by moving Stage 6a after Stage 5, selecting against the latest queue, and including `deferred_to_user` Tyler-routed disputes. Verified with `tests/test_engine.py::test_select_stage6a_steering_disputes_uses_latest_tyler_statuses` and `tests/test_engine.py::test_run_pipeline_collects_guidance_from_stage5_deferred_queue`. | local_divergence | critical | grounded-research | verified_fixed |
| S6-EVIDENCE-CONTEXT-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 6b evidence trail lines 204-207; `3. V1_SCHEMAS.md` EvidenceTrailEntry lines 463-468 and validation rule line 500; `4. V1_PROMPTS.md` Stage 6b key evidence sources lines 1042-1047 | Stage 6 synthesis should receive the sources that contributed to dispute resolution, including Stage 5 targeted-verification sources | `src/grounded_research/export.py` `generate_tyler_synthesis_report()` | Tyler's Stage 6b prompt expects `KEY EVIDENCE SOURCES` to include sources that contributed to disputes and recommendation. The original divergence was that live code built `top_sources` only from `state.evidence_bundle.sources`, which excluded Stage 5 `additional_sources`. Fixed on 2026-04-08 by merging Stage 2 and Stage 5 source inventories into one Stage 6 top-source assembly helper. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_includes_stage5_additional_sources_in_top_sources`. | local_divergence | high | grounded-research | verified_fixed |
| S6-COMPACTION-001 | `1. V1_Build_Plan_Step_By_Step.md` Stage 6b context compaction lines 174-175; `4. V1_PROMPTS.md` Context Compaction lines 882-900 | Stage 6 should compact prior-stage artifacts when synthesis input exceeds ~80K characters, using Tyler's priority order and front/back anchoring | `engine.py` evidence compression call; `src/grounded_research/export.py` `generate_tyler_synthesis_report()` | Tyler specifies a pre-synthesis compaction step triggered by total input size and ordered by claim/dispute importance. The original divergence was that `generate_tyler_synthesis_report()` rendered all Stage 6 inputs directly with no char-budget compaction. Fixed on 2026-04-08 by adding a Stage 6 prompt-input compaction helper that triggers on a configurable char-count limit and compresses lower-priority context first. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_compacts_noncritical_claims_when_over_limit`. | local_divergence | medium | grounded-research | verified_fixed |
| S6-GROUNDING-001 | `3. V1_SCHEMAS.md` Stage 6 validation rules lines 496-504; `1. V1_Build_Plan_Step_By_Step.md` Stage 6b output validation lines 184-186; `4. V1_PROMPTS.md` output validation lines 938-942 | Stage 6 grounding failures should trigger a post-synthesis reject-and-retry once, not only a warning after report generation | `src/grounded_research/export.py` `generate_tyler_synthesis_report()` + `validate_tyler_grounding()`; `engine.py` `run_pipeline()` | Fixed on 2026-04-13 in Phase 2 of the post-audit remediation wave. `generate_tyler_synthesis_report()` now feeds grounding failures back into the live Stage 6 repair loop, retries once with explicit feedback, and raises if validation still fails. `validate_tyler_grounding()` now also enforces Tyler's explicit recommendation-to-claim reference requirement, and `engine.run_pipeline()` treats any residual grounding failure after synthesis as a hard error instead of downgrading it to warnings. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_repairs_grounding_failure`, `tests/test_export.py::test_validate_tyler_grounding_requires_claim_reference_in_recommendation`, `tests/test_export.py`, and `tests/test_engine.py`. | local_divergence | high | grounded-research | verified_fixed |
| S6-VALIDATION-COVERAGE-001 | `3. V1_SCHEMAS.md` Stage 6 validation rules lines 496-504; `4. V1_PROMPTS.md` output validation lines 938-950 | Stage 6 runtime validation should enforce Tyler's explicit final-report checks, not only the current underfilled-field and zombie subset | `src/grounded_research/export.py` `_validate_tyler_synthesis_report()`; `src/grounded_research/tyler_v1_models.py` `SynthesisReport` | Fixed on 2026-04-13 in Phase 2 of the post-audit remediation wave. `_validate_tyler_synthesis_report()` now enforces Tyler's remaining explicit Stage 6 schema checks in the live repair loop: minimum `conditions_of_validity`, valid `supporting_claims` IDs for preserved alternatives, executed-stage coverage in `process_summary`, and a maximum conditional-marker budget in `executive_recommendation`, in addition to the existing tradeoff, preserved-alternative, unresolved-dispute, and zombie checks. The synthesis path now raises if those validation errors remain after the configured repair attempt. Verified with `tests/test_export.py::test_generate_tyler_synthesis_report_repairs_underfilled_decision_fields`, `tests/test_export.py`, and `tests/test_engine.py`. | local_divergence | medium | grounded-research | verified_fixed |
| EXT-SCHEMA-001 | `2. V1_DESIGN.md` Design Constraints lines 52 and 56-57; `3. V1_SCHEMAS.md` Stage 4 ClaimLedgerEntry lines 255-267 and Stage 5 ClaimStatusUpdate lines 353-358 | Additive lineage fields may remain if they do not conflict with Tyler and are documented as extensions rather than literal schema parity | `src/grounded_research/tyler_v1_models.py` `ClaimLedgerEntry`, `ClaimStatusUpdate`; `docs/TYLER_FULL_SPEC_AUDIT_MATRIX.md` | Local schema adds `status_at_extraction`, `is_provisional`, and `basis_for_change` at [tyler_v1_models.py](../src/grounded_research/tyler_v1_models.py#L233) through [tyler_v1_models.py](../src/grounded_research/tyler_v1_models.py#L240) and [tyler_v1_models.py](../src/grounded_research/tyler_v1_models.py#L318) through [tyler_v1_models.py](../src/grounded_research/tyler_v1_models.py#L332). Tyler's schema markdown does not include these exact fields, but the Design packet requires lineage tracking, separation of working notes from locked decisions, and anti-conformity status-change reasons. The audit matrix documents the fields as additive extensions rather than literal schema parity. | extension | low | docs-only | document_extension |
| DOC-S2-QUERY-001 | Anchor exception: local doc-governance row derived from active repo prompt inventory and superseded status docs, not a standalone Tyler requirement; related Tyler query anchors are tracked by `S2-QUERY-MODEL-001`, `S2-QUERY-VARIANTS-001`, and `DOC-S2-QUERY-002` | Status docs should identify the real active Stage 2 query surface | `docs/archive/TYLER_LITERAL_PROMPT_FIDELITY_AUDIT.md`; `docs/archive/TYLER_LITERAL_PARITY_AUDIT.md`; `docs/TYLER_EXECUTION_STATUS.md` | Fixed on 2026-04-08 by landing the then-missing query-diversification prompt and reconciling status docs. Superseded on 2026-06-25 by `S2-QUERY-MODEL-001`: the active Stage 2 query surface is now deterministic orchestrator templates, and the obsolete query-diversification prompt has been deleted. | stale_doc | high | docs-only | verified_fixed |
| DOC-S2-QUERY-002 | Tyler Build Plan Stage 2 Step 1 line 38, Design Stage 2/Source Strategy lines 93-95 and 269-273, and Prompt packet Stage 2 query variant generation lines 263-285 | Active status docs and audit trackers must not claim the live model-driven Stage 2 query-diversification path is Tyler-literal while `S2-QUERY-MODEL-001` and `S2-QUERY-VARIANTS-001` are reopened | `docs/TYLER_EXECUTION_STATUS.md`; `docs/TYLER_FULL_SPEC_AUDIT_MATRIX.md`; `docs/TYLER_SYSTEMATIC_REVIEW_MATRIX.md`; `docs/PLAN.md`; `docs/ROADMAP.md` | Reconciled on 2026-06-25 after restoring deterministic Stage 2 query templates. Active status and audit docs now describe the live deterministic template path, not the superseded model-driven query path. | stale_doc | high | docs-only | verified_fixed |
| DOC-S3-MODEL-001 | Anchor exception: local doc-governance row about status-doc truthfulness; related Tyler Stage 3 model anchors are tracked by `S3-FRAME-MODEL-001`, `AMB-S3-FRAME-001`, and `S3-MODEL-VERSION-001` | If Stage 3 model assignments are not Tyler-literal, status docs must not claim they are | `docs/TYLER_EXECUTION_STATUS.md` | Fixed on 2026-04-08 by updating the default B/C frame-model mapping and reconciling the status surface so it distinguishes fixed local frame ordering from still-open exact frontier-model version parity. | stale_doc | medium | docs-only | verified_fixed |
| DOC-ROADMAP-001 | Anchor exception: local doc-governance row about active roadmap drift against the ledger and audit/status surfaces, not a standalone Tyler requirement | The roadmap current-state summary must not describe pre-audit architecture that the ledger later proved false | `docs/ROADMAP.md` | Fixed on 2026-04-09 by rewriting the roadmap current-state summary to describe the actual Tyler-native runtime, the satisfied three-case eval gate, the merged Gemini shared fix, and the still-open frontier model-version / model-policy rows instead of the earlier pre-audit architecture snapshot. | stale_doc | medium | docs-only | verified_fixed |
| DOC-README-001 | README lines 5-7 and 77-79; Tyler packet plus active audit/status surfaces | README must not describe Stage 3 model parity as a generic “closest available” note once the live family/role mapping has been corrected and the remaining gap is only exact Gemini version parity | `README.md` | Fixed on 2026-04-09 by updating README feature notes to distinguish the landed Stage 3 family/role mapping from the remaining exact Gemini 3.1 Pro substitution row, and by pointing current Tyler code-vs-spec status to `docs/TYLER_SPEC_GAP_LEDGER.md` rather than treating README as the authority. | stale_doc | medium | docs-only | verified_fixed |
| DOC-FEATURE-001 | Anchor exception: local doc-governance row about active feature-status drift against the ledger and audit/status surfaces, not a standalone Tyler requirement | Feature status notes must not preserve pre-audit runtime descriptions when the live Stage 2/3/6 behavior has changed materially | `docs/FEATURE_STATUS.md` | Fixed on 2026-04-09 by updating the assessment date and correcting the Stage 2 findings, Stage 3 analyst-model, and Stage 6 compaction notes to match the current Tyler-native runtime and open exact-model gap. | stale_doc | medium | docs-only | verified_fixed |
| DOC-STATUS-001 | Anchor exception: local doc-governance row about active status and ownership doc drift; related Tyler model-version anchors are tracked by `S3-MODEL-VERSION-001` and `STATUS-FRONTIER-RUNTIME-001` | Active status and ownership docs must name the exact remaining model-version parity row separately from the stochastic frontier reliability/policy row | `docs/TYLER_EXECUTION_STATUS.md`; `docs/TYLER_SHARED_INFRA_OWNERSHIP.md`; `docs/plans/archive/tyler_faithful_execution_remainder.md` | Fixed on 2026-04-09 by splitting the exact Tyler Gemini model-version parity note from the separate frontier runtime/model-policy note and by pointing the exact-model row at a dedicated shared follow-up plan. | stale_doc | medium | docs-only | verified_fixed |
